import { motion, useScroll, useTransform } from "framer-motion";
import useEffect from "React"
const Scroll = () => {
  useEfect
  return <motion.div style={{ y: scrollY }}>Scroll Me</motion.div>;
};

export default Scroll;
